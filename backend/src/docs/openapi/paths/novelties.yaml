paths:
  /api/novelties:
    get:
      tags:
        - Novelties
      summary: Obtener feed de novedades
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
        - in: query
          name: offset
          required: false
          schema:
            type: integer
      responses:
        '200':
          description: Feed de novedades
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DataResponseGeneric'
        '401':
          description: No autenticado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
